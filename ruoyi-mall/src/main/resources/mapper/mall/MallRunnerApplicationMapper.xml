<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.mall.mapper.MallRunnerApplicationMapper">

    <resultMap type="com.ruoyi.mall.domain.MallRunnerApplication" id="AppResult">
        <id     property="appId"        column="app_id"        />
        <result property="memberId"     column="member_id"     />
        <result property="realName"     column="real_name"     />
        <result property="idNumber"     column="id_number"     />
        <result property="phone"        column="phone"         />
        <result property="idPhotoUrl"   column="id_photo_url"  />
        <result property="status"       column="status"        />
        <result property="rejectReason" column="reject_reason" />
        <result property="applyTime"    column="apply_time"    />
        <result property="reviewTime"   column="review_time"   />
        <result property="reviewer"     column="reviewer"      />
        <result property="isOnline"     column="is_online"     />
        <result property="storeId"      column="store_id"      />
        <result property="nickName"     column="nick_name"     />
        <result property="avatarUrl"    column="avatar_url"    />
        <result property="storeName"    column="store_name"    />
    </resultMap>

    <select id="selectByMemberId" parameterType="Long" resultMap="AppResult">
        select a.*, m.nick_name, m.avatar_url, s.name as store_name
        from mall_runner_application a
        left join mall_member m on m.member_id = a.member_id
        left join mall_store s on s.store_id = a.store_id
        where a.member_id = #{memberId}
    </select>

    <select id="selectById" parameterType="Long" resultMap="AppResult">
        select a.*, m.nick_name, m.avatar_url, s.name as store_name
        from mall_runner_application a
        left join mall_member m on m.member_id = a.member_id
        left join mall_store s on s.store_id = a.store_id
        where a.app_id = #{appId}
    </select>

    <select id="selectList" parameterType="com.ruoyi.mall.domain.MallRunnerApplication" resultMap="AppResult">
        select a.*, m.nick_name, m.avatar_url, s.name as store_name
        from mall_runner_application a
        left join mall_member m on m.member_id = a.member_id
        left join mall_store s on s.store_id = a.store_id
        <where>
            <if test="status != null and status != ''">AND a.status = #{status}</if>
            <if test="realName != null and realName != ''">AND a.real_name like concat('%', #{realName}, '%')</if>
        </where>
        order by a.apply_time desc
    </select>

    <insert id="insertOrUpdate" parameterType="com.ruoyi.mall.domain.MallRunnerApplication">
        insert into mall_runner_application
            (member_id, real_name, id_number, phone, id_photo_url, status, apply_time)
        values
            (#{memberId}, #{realName}, #{idNumber}, #{phone}, #{idPhotoUrl}, '0', #{applyTime})
        on duplicate key update
            real_name    = values(real_name),
            id_number    = values(id_number),
            phone        = values(phone),
            id_photo_url = values(id_photo_url),
            status       = '0',
            reject_reason = null,
            apply_time   = values(apply_time)
    </insert>

    <update id="updateStatus" parameterType="com.ruoyi.mall.domain.MallRunnerApplication">
        update mall_runner_application
        set status        = #{status},
            reject_reason = #{rejectReason},
            review_time   = #{reviewTime},
            reviewer      = #{reviewer}
        where app_id = #{appId}
    </update>

    <update id="updateOnlineStatus">
        update mall_runner_application set is_online = #{isOnline} where member_id = #{memberId}
    </update>

    <update id="updateStore">
        update mall_runner_application set store_id = #{storeId} where app_id = #{appId}
    </update>

</mapper>
