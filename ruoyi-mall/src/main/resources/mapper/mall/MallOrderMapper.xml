<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.mall.mapper.MallOrderMapper">

    <resultMap type="com.ruoyi.mall.domain.MallOrder" id="MallOrderResult">
        <id     property="orderId"          column="order_id"          />
        <result property="orderNo"          column="order_no"          />
        <result property="memberId"         column="member_id"         />
        <result property="addressSnapshot"  column="address_snapshot"  />
        <result property="totalAmount"      column="total_amount"      />
        <result property="status"           column="status"            />
        <result property="remark"           column="remark"            />
        <result property="paymentMethod"    column="payment_method"    />
        <result property="paymentStatus"    column="payment_status"    />
        <result property="paymentNo"        column="payment_no"        />
        <result property="paidAmount"       column="paid_amount"       />
        <result property="paymentTime"      column="payment_time"      />
        <result property="cancelReason"     column="cancel_reason"     />
        <result property="orderSource"        column="order_source"         />
        <result property="createTime"         column="create_time"          />
        <result property="updateBy"           column="update_by"            />
        <result property="updateTime"         column="update_time"          />
        <result property="runnerMemberId"     column="runner_member_id"     />
        <result property="runnerAcceptedTime" column="runner_accepted_time" />
        <result property="deliveryFee"        column="delivery_fee"         />
        <result property="runnerFeeSettled"   column="runner_fee_settled"   />
        <result property="pointsUsed"         column="points_used"          />
        <result property="memberCouponId"     column="member_coupon_id"     />
        <result property="couponDiscount"     column="coupon_discount"      />
        <result property="tenderType"         column="tender_type"          />
        <result property="cashReceived"       column="cash_received"        />
        <result property="changeDue"          column="change_due"           />
        <result property="cashierId"          column="cashier_id"           />
        <result property="storeId"            column="store_id"             />
        <result property="merchantOutTradeNo" column="merchant_out_trade_no" />
        <result property="subsidy"            column="subsidy"              />
        <result property="arrivalTime"        column="arrival_time"         />
    </resultMap>

    <sql id="selectOrderVo">
        select order_id, order_no, member_id, address_snapshot, total_amount, status,
               remark, payment_method, payment_status, payment_no, paid_amount, payment_time,
               cancel_reason, order_source, create_time, update_by, update_time,
               runner_member_id, runner_accepted_time, delivery_fee, runner_fee_settled,
               points_used, member_coupon_id, coupon_discount,
               tender_type, cash_received, change_due, cashier_id, store_id,
               merchant_out_trade_no, subsidy, arrival_time
        from mall_order
    </sql>

    <select id="selectOrderList" parameterType="com.ruoyi.mall.domain.MallOrder" resultMap="MallOrderResult">
        <include refid="selectOrderVo"/>
        <where>
            <if test="memberId != null">AND member_id = #{memberId}</if>
            <if test="status != null and status != ''">AND status = #{status}</if>
            <if test="orderNo != null and orderNo != ''">AND order_no like concat('%', #{orderNo}, '%')</if>
            <if test="orderSource != null and orderSource != ''">AND order_source = #{orderSource}</if>
            <if test="paymentMethod != null and paymentMethod != ''">AND payment_method = #{paymentMethod}</if>
        </where>
        order by create_time desc
    </select>

    <select id="selectOrderById" parameterType="Long" resultMap="MallOrderResult">
        <include refid="selectOrderVo"/> where order_id = #{orderId}
    </select>

    <select id="selectOrderByOrderNo" parameterType="String" resultMap="MallOrderResult">
        <include refid="selectOrderVo"/> where order_no = #{orderNo}
    </select>

    <select id="selectOrderByOrderNoForUpdate" parameterType="String" resultMap="MallOrderResult">
        <include refid="selectOrderVo"/> where order_no = #{orderNo} for update
    </select>

    <insert id="insertOrder" parameterType="com.ruoyi.mall.domain.MallOrder" useGeneratedKeys="true" keyProperty="orderId">
        insert into mall_order(order_no, member_id, address_snapshot, total_amount, status, remark,
                               payment_method, payment_status, order_source, points_used,
                               member_coupon_id, coupon_discount, delivery_fee, store_id, create_time)
        values(#{orderNo}, #{memberId}, #{addressSnapshot}, #{totalAmount}, #{status}, #{remark},
               #{paymentMethod}, 'UNPAID', #{orderSource}, ifnull(#{pointsUsed}, 0),
               #{memberCouponId}, ifnull(#{couponDiscount}, 0), ifnull(#{deliveryFee}, 0), #{storeId}, #{createTime})
    </insert>

    <update id="updateOrder" parameterType="com.ruoyi.mall.domain.MallOrder">
        update mall_order
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="paymentStatus != null">payment_status = #{paymentStatus},</if>
            <if test="paymentNo != null">payment_no = #{paymentNo},</if>
            <if test="paidAmount != null">paid_amount = #{paidAmount},</if>
            <if test="paymentTime != null">payment_time = #{paymentTime},</if>
            <if test="cancelReason != null">cancel_reason = #{cancelReason},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            update_time = #{updateTime}
        </set>
        where order_id = #{orderId}
    </update>

    <update id="updateAggregateInfo">
        update mall_order
        set merchant_out_trade_no = #{merchantOutTradeNo},
            subsidy = #{subsidy},
            update_time = now()
        where order_id = #{orderId}
    </update>

    <!-- ── 外部导入单 ── -->

    <select id="selectOrderByMerchantOutTradeNo" parameterType="String" resultMap="MallOrderResult">
        <include refid="selectOrderVo"/>
        where merchant_out_trade_no = #{merchantOutTradeNo}
          and order_source = 'EXTERNAL'
        limit 1
    </select>

    <!-- 导入单模拟接单：直写 runner + 接单时间 + 到达时间 + 状态（绕开 updateRunnerInfo 的 store/乐观锁语义） -->
    <update id="assignImportRunner">
        update mall_order
        set runner_member_id     = #{runnerMemberId},
            runner_accepted_time = #{acceptedTime},
            arrival_time         = #{arrivalTime},
            status               = #{status},
            update_time          = #{updateTime}
        where order_id = #{orderId}
    </update>

    <!-- 扫描到点(到达时间已过)仍处于配送中的导入单 -->
    <select id="selectArrivedImports" resultType="Long">
        select order_id from mall_order
        where status = '2' and order_source = 'EXTERNAL'
          and arrival_time is not null and arrival_time &lt;= now()
        order by arrival_time asc
        limit #{limit}
    </select>

    <!-- 到点完成：置已完成，完成时刻取到达时间 -->
    <update id="completeArrived">
        update mall_order
        set status = '3', update_time = arrival_time
        where order_id = #{orderId} and status = '2' and order_source = 'EXTERNAL'
    </update>

    <update id="updatePosTender" parameterType="com.ruoyi.mall.domain.MallOrder">
        update mall_order
        set tender_type   = #{tenderType},
            cash_received = #{cashReceived},
            change_due    = #{changeDue},
            cashier_id    = #{cashierId},
            update_time   = #{updateTime}
        where order_id = #{orderId}
    </update>

    <select id="selectAvailableForRunner" parameterType="Long" resultMap="MallOrderResult">
        <include refid="selectOrderVo"/>
        where status = '1' and runner_member_id is null
        <!-- 骑手已绑定门店则只看本店的单；未绑定(null)则看全部，兼容存量骑手 -->
        <if test="storeId != null">and store_id = #{storeId}</if>
        order by create_time asc
    </select>

    <select id="selectByRunnerMemberId" parameterType="Long" resultMap="MallOrderResult">
        <include refid="selectOrderVo"/>
        where runner_member_id = #{runnerMemberId}
        order by runner_accepted_time desc
    </select>

    <select id="countByRunnerMemberId" parameterType="Long" resultType="int">
        select count(1) from mall_order where runner_member_id = #{runnerMemberId}
    </select>

    <update id="updateRunnerInfo" parameterType="com.ruoyi.mall.domain.MallOrder">
        update mall_order
        set runner_member_id     = #{runnerMemberId},
            runner_accepted_time = #{runnerAcceptedTime},
            delivery_fee         = #{deliveryFee},
            status               = #{status}
        where order_id = #{orderId}
          and runner_member_id is null
    </update>

    <select id="selectUnsettledRunnerOrders" parameterType="Long" resultMap="MallOrderResult">
        <include refid="selectOrderVo"/>
        where payment_method = 'GCASH'
          and runner_member_id is not null
          and runner_fee_settled = '0'
          and status = '3'
          <if test="_parameter != null">and runner_member_id = #{runnerMemberId}</if>
        order by runner_member_id, create_time desc
    </select>

    <update id="settleRunnerFee">
        update mall_order set runner_fee_settled = '1'
        where order_id in
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <select id="countDeliveriesThisWeek" parameterType="Long" resultType="int">
        select count(1) from mall_order
        where runner_member_id = #{runnerMemberId} and status = '3'
          and YEARWEEK(update_time, 1) = YEARWEEK(NOW(), 1)
    </select>

    <select id="countDeliveriesThisMonth" parameterType="Long" resultType="int">
        select count(1) from mall_order
        where runner_member_id = #{runnerMemberId} and status = '3'
          and DATE_FORMAT(update_time, '%Y-%m') = DATE_FORMAT(NOW(), '%Y-%m')
    </select>

    <select id="sumEarningsThisWeek" parameterType="Long" resultType="java.math.BigDecimal">
        select COALESCE(SUM(delivery_fee), 0) from mall_order
        where runner_member_id = #{runnerMemberId} and status = '3'
          and YEARWEEK(update_time, 1) = YEARWEEK(NOW(), 1)
    </select>

    <select id="sumEarningsThisMonth" parameterType="Long" resultType="java.math.BigDecimal">
        select COALESCE(SUM(delivery_fee), 0) from mall_order
        where runner_member_id = #{runnerMemberId} and status = '3'
          and DATE_FORMAT(update_time, '%Y-%m') = DATE_FORMAT(NOW(), '%Y-%m')
    </select>

    <select id="countCompletedByMemberId" parameterType="Long" resultType="int">
        select COUNT(*) from mall_order where member_id = #{memberId} and status = '3'
    </select>

    <select id="countAllByMemberId" parameterType="Long" resultType="int">
        select COUNT(*) from mall_order where member_id = #{memberId}
    </select>

</mapper>
