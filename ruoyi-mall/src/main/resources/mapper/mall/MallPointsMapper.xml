<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.mall.mapper.MallPointsMapper">

    <resultMap type="com.ruoyi.mall.domain.MallPointsLog" id="PointsLogResult">
        <id     property="logId"       column="log_id"        />
        <result property="memberId"    column="member_id"     />
        <result property="delta"       column="delta"         />
        <result property="balanceAfter" column="balance_after"/>
        <result property="source"      column="source"        />
        <result property="sourceId"    column="source_id"     />
        <result property="remark"      column="remark"        />
        <result property="createTime"  column="create_time"   />
    </resultMap>

    <!-- 原子加减积分，返回更新行数（1=成功，0=余额不足被拒绝） -->
    <update id="addPoints">
        UPDATE mall_member
        SET points = points + #{delta}
        WHERE member_id = #{memberId}
          AND points + #{delta} >= 0
    </update>

    <select id="selectBalance" resultType="int">
        SELECT COALESCE(points, 0) FROM mall_member WHERE member_id = #{memberId}
    </select>

    <insert id="insertLog" parameterType="com.ruoyi.mall.domain.MallPointsLog" useGeneratedKeys="true" keyProperty="logId">
        INSERT INTO mall_points_log(member_id, delta, balance_after, source, source_id, remark, create_time)
        VALUES(#{memberId}, #{delta}, #{balanceAfter}, #{source}, #{sourceId}, #{remark}, #{createTime})
    </insert>

    <select id="selectLogsByMemberId" resultMap="PointsLogResult">
        SELECT log_id, member_id, delta, balance_after, source, source_id, remark, create_time
        FROM mall_points_log
        WHERE member_id = #{memberId}
        ORDER BY create_time DESC
        LIMIT 50
    </select>

</mapper>
